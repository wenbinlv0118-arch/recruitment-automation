# 前端简历解析数据格式规范

## 问题分析

根据当前的数据库结构和后端处理逻辑，前端在进行简历解析时需要输出特定格式的数据才能被正确存储到数据库中。

### 当前问题
1. 前端解析后的 `parsedResume` 数据结构与后端期望的格式不匹配
2. 后端 `addResume` 方法期望接收包含 `basicInfo`、`jobIntention` 等标准化字段的数据
3. 前端目前可能返回的是旧格式或不完整的数据结构

## 标准数据格式

前端简历解析完成后，应该返回以下标准格式的数据：

```javascript
{
  // 基本信息（必需）
  "basicInfo": {
    "name": "张三",                    // 姓名
    "gender": "男",                   // 性别
    "age": 28,                        // 年龄（数字）
    "phone": "13800138000",           // 手机号
    "email": "zhangsan@example.com",  // 邮箱
    "address": "北京市朝阳区",          // 地址
    "currentStatus": "离职-随时到岗",   // 当前状态
    "selfEvaluation": "具有5年工作经验..." // 自我评价
  },
  
  // 求职意向（必需）
  "jobIntention": {
    "expectedPosition": "前端开发工程师", // 期望职位
    "expectedSalary": "15-25K",        // 期望薪资
    "expectedLocation": "北京",         // 期望工作地点
    "jobType": "全职"                   // 工作类型
  },
  
  // 工作经历（数组）
  "workExperiences": [
    {
      "company": "阿里巴巴",              // 公司名称
      "position": "高级前端开发工程师",    // 职位
      "startDate": "2020-03",           // 开始时间
      "endDate": "2023-08",             // 结束时间
      "description": "负责前端架构设计..." // 工作描述
    }
  ],
  
  // 项目经历（数组，可选）
  "projectExperiences": [
    {
      "name": "电商平台前端重构",          // 项目名称
      "role": "前端负责人",              // 项目角色
      "startDate": "2022-01",           // 开始时间
      "endDate": "2022-06",             // 结束时间
      "description": "负责整个前端架构...", // 项目描述
      "technologies": "React, TypeScript" // 使用技术
    }
  ],
  
  // 教育经历（数组）
  "educationExperiences": [
    {
      "school": "北京大学",               // 学校名称
      "major": "计算机科学与技术",         // 专业
      "degree": "本科",                  // 学历
      "startDate": "2015-09",           // 开始时间
      "endDate": "2019-06",             // 结束时间
      "gpa": "3.8"                      // GPA（可选）
    }
  ],
  
  // 语言技能（数组，可选）
  "languageSkills": [
    {
      "languageName": "英语",            // 语言名称
      "proficiencyLevel": "熟练",        // 熟练程度
      "certificates": "CET-6"           // 相关证书
    }
  ],
  
  // 技能标签（数组）
  "skills": [
    {
      "name": "JavaScript",             // 技能名称
      "level": "精通",                   // 熟练程度
      "category": "编程语言"             // 技能分类
    },
    {
      "name": "React",
      "level": "熟练",
      "category": "前端框架"
    }
  ],
  
  // 证书资质（数组，可选）
  "certificates": [
    {
      "name": "PMP项目管理认证",          // 证书名称
      "issuingOrganization": "PMI",     // 颁发机构
      "issueDate": "2022-05",           // 获得时间
      "expiryDate": "2025-05",          // 过期时间
      "credentialId": "PMP123456"       // 证书编号
    }
  ],
  
  // 元数据（系统字段）
  "source": "boss",                    // 简历来源
  "filename": "张三_简历.pdf",          // 文件名（如果是文件上传）
  "file_path": "/path/to/file.pdf",   // 文件路径（如果是文件上传）
  "quality_score": 85,                // 质量评分
  "status": "pending"                 // 状态
}
```

## 前端实现建议

### 1. 修改简历解析逻辑

在前端简历解析组件中，确保解析后返回上述标准格式：

```javascript
// 在 ResumeUploadModal.js 中
const handleBossResumeParseText = useCallback(async () => {
  const textValue = form.getFieldValue('bossResumeText');
  if (!textValue || textValue.trim().length < 50) {
    message.warning('请输入足够的Boss直聘简历文本内容（至少50字符）');
    return;
  }
  
  setLoading(true);
  try {
    // 调用后端解析接口
    const response = await fetch('/api/resume/parse-text', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ text: textValue })
    });
    
    const result = await response.json();
    
    if (result.success) {
      // 设置标准格式的解析结果
      setParsedResume(result.data);
      message.success('简历解析成功');
    } else {
      message.error(result.error || '简历解析失败');
    }
  } catch (error) {
    console.error('简历解析失败:', error);
    message.error('简历解析失败，请重试');
  } finally {
    setLoading(false);
  }
}, [form]);
```

### 2. 确保提交数据格式正确

在提交简历时，确保发送的数据包含所有必需字段：

```javascript
const handleSubmit = useCallback(async () => {
  try {
    const values = await form.validateFields();
    
    if (!parsedResume) {
      message.warning('请先解析简历');
      return;
    }
    
    setLoading(true);
    
    // 构造标准格式的简历数据
    const resumeData = {
      // 基本信息（必需）
      basicInfo: parsedResume.basicInfo || {},
      // 求职意向（必需）
      jobIntention: parsedResume.jobIntention || {},
      // 其他信息
      workExperiences: parsedResume.workExperiences || [],
      projectExperiences: parsedResume.projectExperiences || [],
      educationExperiences: parsedResume.educationExperiences || [],
      languageSkills: parsedResume.languageSkills || [],
      skills: parsedResume.skills || [],
      certificates: parsedResume.certificates || [],
      // 元数据
      source: values.source,
      notes: values.notes,
      filename: parsedResume.filename,
      file_path: parsedResume.file_path,
      quality_score: parsedResume.quality_score || 0,
      status: 'pending'
    };
    
    console.log('提交的简历数据:', resumeData);
    
    const response = await fetch('/api/resume-library', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(resumeData)
    });
    
    const result = await response.json();
    
    if (result.success) {
      message.success('简历添加成功');
      onSuccess && onSuccess(result.data);
      handleClose();
    } else {
      message.error(result.error || '简历添加失败');
    }
  } catch (error) {
    console.error('简历添加失败:', error);
    message.error('简历添加失败，请重试');
  } finally {
    setLoading(false);
  }
}, [form, parsedResume, onSuccess]);
```

## 后端接口说明

### 1. 文本解析接口

**接口：** `POST /api/resume/parse-text`

**请求参数：**
```javascript
{
  "text": "简历文本内容"
}
```

**返回格式：**
```javascript
{
  "success": true,
  "data": {
    // 标准格式的简历数据
    "basicInfo": { ... },
    "jobIntention": { ... },
    // ... 其他字段
  },
  "validation": {
    "isValid": true,
    "errors": []
  },
  "parseMethod": "boss_format"
}
```

### 2. 简历添加接口

**接口：** `POST /api/resume-library`

**请求参数：** 标准格式的简历数据（如上所述）

**返回格式：**
```javascript
{
  "success": true,
  "data": {
    "id": "uuid",
    // 完整的简历信息
  }
}
```

## 注意事项

1. **必需字段：** `basicInfo` 和 `jobIntention` 是必需的，即使为空对象也要包含
2. **数据类型：** 注意年龄等数字字段的类型
3. **数组字段：** 所有数组字段如果没有数据应该传空数组 `[]` 而不是 `null`
4. **日期格式：** 日期建议使用 `YYYY-MM` 格式
5. **验证：** 后端会进行数据验证，确保前端数据格式正确

## 测试建议

1. 使用标准格式的测试数据验证整个流程
2. 测试各种边界情况（空字段、缺失字段等）
3. 验证数据库存储和前端显示的一致性
4. 测试不同简历来源的数据格式兼容性