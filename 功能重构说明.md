# 企业智库功能重构说明

## 重构概述

根据需求，我们对"企业智库"功能进行了重构，将智能检索和AI对话功能集成到"与Moirai对话"中，实现了更统一和便捷的用户体验。

## 主要变更

### 1. 企业智库功能简化
- **保留功能**：
  - 文档管理：查看和管理已上传的文档，支持查看、删除操作（带确认弹窗）
  - 文档上传：支持多种格式的文档上传

- **移除功能**：
  - 智能检索（已集成到Moirai对话）
  - AI对话（已集成到Moirai对话）

### 2. 导航栏优化
- 将"企业智库"改为可展开的子菜单
- 包含两个子功能：
  - 文档管理（5-1）
  - 文档上传（5-2）

### 3. Moirai对话功能增强
- **新增功能**：
  - 知识库检索：支持自然语言查询企业文档
  - 知识库AI对话：基于企业知识库进行智能对话

- **触发方式**：
  - 知识库检索：包含"文档"、"知识"、"资料"、"文件"、"检索"、"搜索"、"查询"、"查找"等关键词
  - 知识库AI对话：包含"AI"、"助手"、"对话"等关键词

## 使用指南

### 企业智库使用
1. 点击左侧导航栏的"企业智库"
2. 展开后选择：
   - "文档管理"：查看已上传的文档列表，支持查看、删除操作
   - "文档上传"：上传新的文档到知识库

### 文档查看操作
1. 在文档管理页面，点击文档行的"查看"按钮
2. 系统会以弹窗形式显示文档内容，采用"弹窗基础配置"样式
3. 弹窗包含文档信息、内容预览和下载功能
4. 支持查看文档的完整内容，包括所有文档块

### 文档删除操作
1. 在文档管理页面，点击文档行的"删除"按钮
2. 系统会弹出确认对话框，显示文档名称和警告信息
3. 确认删除后，文档及其相关内容将被永久删除
4. 删除的文档不再出现在知识库检索结果中

### 知识库检索使用
在"与Moirai对话"中输入包含以下关键词的问题：
- "查找关于产品开发的文档"
- "搜索技术规范资料"
- "查询公司政策文件"

### 知识库AI对话使用
在"与Moirai对话"中输入包含以下关键词的问题：
- "AI助手，帮我分析一下这个技术方案"
- "基于企业知识库，回答关于项目管理的问题"
- "对话模式，解释一下公司的招聘流程"

## 技术实现

### 前端变更
- `App.js`：修改导航菜单结构，添加知识库查询处理逻辑，移除不需要的组件导入
- `KnowledgeBase.js`：简化组件，移除内部侧边栏，直接根据传入参数显示对应内容，新增文档查看、删除功能
- `DocumentViewer.js`：新增文档查看组件，采用"弹窗基础配置"样式，支持文档内容预览和下载
- 移除旧的独立组件：`AIChat.js`、`SmartSearch.js`（功能已集成到主对话中）

### 后端变更
- `index.js`：添加知识库服务的socket处理
- 新增`knowledgeChat`事件处理
- 集成知识库检索和AI对话功能

### API支持
- 知识库检索：`POST /api/knowledge/retrieve`
- 知识库AI对话：`POST /api/knowledge/chat`
- 文档管理：`GET /api/knowledge/documents`
- 文档上传：`POST /api/knowledge/upload`
- 文档查看：`GET /api/knowledge/documents/:id`、`GET /api/knowledge/documents/:id/chunks`
- 文档下载：`GET /api/knowledge/documents/:id/download`
- 文档删除：`DELETE /api/knowledge/documents/:id`

## 用户体验优化

1. **统一入口**：所有对话功能都通过"与Moirai对话"进行
2. **智能识别**：系统自动识别用户意图，选择合适的处理方式
3. **自然交互**：支持自然语言查询，无需记忆特定命令
4. **上下文感知**：AI对话基于企业知识库，提供更准确的回答

## 测试建议

1. **文档上传测试**：上传不同类型的文档，验证处理效果
2. **检索功能测试**：使用不同关键词测试知识库检索
3. **AI对话测试**：测试基于知识库的智能对话
4. **集成测试**：验证所有功能在统一界面下的协作效果 