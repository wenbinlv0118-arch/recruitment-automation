# ARM64优化配置
target_arch=arm64
target_platform=darwin
# 使用 --prefer-online 替代 cache-max
prefer-online=true
engine-strict=true
# 使用 --omit=optional 替代 optional=false
omit=optional
# 使用更快的镜像
registry=https://registry.npmmirror.com/
